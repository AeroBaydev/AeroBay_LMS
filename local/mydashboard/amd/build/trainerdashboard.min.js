define("local_mydashboard/trainerdashboard",[],function(){var selectors={unreadRow:'[data-region="trainer-dashboard-unread-row"]',unreadBadge:'[data-region="trainer-dashboard-unread-badge"]',unreadLabel:'[data-region="trainer-dashboard-unread-label"]'},state={unreadUrl:"",sesskey:""},updateUnreadBadge=function(count){var unread=Math.max(0,parseInt(count,10)||0),row=document.querySelector(selectors.unreadRow),badge=document.querySelector(selectors.unreadBadge),label=document.querySelector(selectors.unreadLabel);badge&&(badge.textContent=unread),label&&(label.textContent=1===unread?"unread message":"unread messages"),row&&(row.hidden=0===unread)},loadUnreadCount=function(){if(!state.unreadUrl)return Promise.resolve();var url=state.unreadUrl+"?sesskey="+encodeURIComponent(state.sesskey);return fetch(url,{credentials:"same-origin"}).then(function(response){return response.json()}).then(function(data){data&&data.success&&updateUnreadBadge(data.unread)}).catch(function(){})};return{init:function(config){state.unreadUrl=config.unreadurl||"",state.sesskey=config.sesskey||"",loadUnreadCount(),window.setInterval(loadUnreadCount,3e4)}}});
